import React, { Component } from 'react';
import './Hosts.css';

class Hosts extends Component {
  render() {
    return (
      <div>
        page Hosts
      </div>
    );
  }
}

export default Hosts;